name: Live Health Check (6h)

on:
  schedule:
    - cron: '0 */6 * * *'  # every 6 hours
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check 9 pages + /policy/ + critical assets
        run: |
          set -e
          URLS=(
            "https://love-8r5.pages.dev/"
            "https://love-8r5.pages.dev/story"
            "https://love-8r5.pages.dev/atmosphere"
            "https://love-8r5.pages.dev/first-visit"
            "https://love-8r5.pages.dev/access"
            "https://love-8r5.pages.dev/review"
            "https://love-8r5.pages.dev/faq"
            "https://love-8r5.pages.dev/contact"
            "https://love-8r5.pages.dev/policy/"
            "https://love-8r5.pages.dev/bulgwang-guide"
            "https://love-8r5.pages.dev/sitemap.xml"
            "https://love-8r5.pages.dev/robots.txt"
            "https://love-8r5.pages.dev/llms.txt"
            "https://love-8r5.pages.dev/site.webmanifest"
            "https://love-8r5.pages.dev/sw.js"
            "https://love-8r5.pages.dev/icon-192.png"
            "https://love-8r5.pages.dev/icon-512.png"
            "https://love-8r5.pages.dev/og/thumb-home.png"
          )
          FAIL=0
          for U in "${URLS[@]}"; do
            CODE=$(curl -sL -o /dev/null -w "%{http_code}" "$U")
            if [ "$CODE" != "200" ]; then
              echo "[FAIL] $U → $CODE"
              FAIL=$((FAIL+1))
            else
              echo "[OK]   $U → 200"
            fi
          done

      - name: Live content guard — banned words must be 0
        run: |
          BANNED=("밤문화" "유흥" "2차로" "AggregateRating")
          FAIL=0
          for U in "https://love-8r5.pages.dev/" \
                   "https://love-8r5.pages.dev/story" \
                   "https://love-8r5.pages.dev/atmosphere" \
                   "https://love-8r5.pages.dev/first-visit" \
                   "https://love-8r5.pages.dev/access" \
                   "https://love-8r5.pages.dev/review" \
                   "https://love-8r5.pages.dev/faq" \
                   "https://love-8r5.pages.dev/contact" \
                   "https://love-8r5.pages.dev/bulgwang-guide"; do
            BODY=$(curl -sL "$U")
            for W in "${BANNED[@]}"; do
              COUNT=$(echo "$BODY" | grep -c "$W" || true)
              if [ "$COUNT" != "0" ]; then
                echo "[FAIL] $U contains '$W' x$COUNT"
                FAIL=$((FAIL+1))
              fi
            done
          done
          if [ "$FAIL" != "0" ]; then
            echo "Total content-guard failures: $FAIL"
            exit 1
          fi
          echo "All live pages clean."
